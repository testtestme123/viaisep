请根据以下数据模型上下文，为项目 {project_id} 生成代码和测试。

目标节点（需要重新生成或检查的模块/能力）：
{target_nodes}

数据模型上下文：
```json
{context}
```

要求：
- 遵守 system.txt 中的 Karpathy 编码准则（简约至上、外科手术式变更、目标驱动执行）
- 只为目标节点及其直接依赖生成文件。
- 不要修改未变更模块的实现，但可以在需要时 import 它们。
- 每个目标节点生成一个 `{{src_file_pattern}}` 和一个 `{{test_file_pattern}}`。
- 测试必须先写，覆盖决策表中与目标节点相关的规则。
- 返回严格 JSON 格式（包含 `files` 和 `discovered` 两个字段，详见 system.txt 输出格式说明）。
- 在生成的代码中为每个验证点附加 verify: [检查项] 注释。
- 如果代码生成过程中发现了图上未建模的新模块、依赖、规则或本体概念，请在 `discovered` 块中返回。
